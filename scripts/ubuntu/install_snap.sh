#!/bin/bash

printf '\n\nInstalling Snap dependencies...\n\n';
sudo snap install typora;
sudo snap install gimp;
sudo snap install spotify;
sudo snap install chromium;
sudo snap install --classic phpstorm;
